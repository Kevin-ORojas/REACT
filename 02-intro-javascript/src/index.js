const activo = false;

// const mensaje = !activo ? "esta activo" : "estado no activo";

const mensaje = !activo && "estado activo"; // este operador es una forma corta de hacer un IF o un = ?   :

console.log(mensaje);
